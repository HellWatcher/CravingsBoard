# Changelog

## v0.1.0 — Initial release

- Per-cook player tracking via ViewEditor with GamePickerList(IAlias)
- `/cravingsboard config` — open player picker
- `/cravingsboard list` — view aggregated cravings
- JSON config persistence per cook
